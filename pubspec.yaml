name: http_cookie_store
description: Process and store cookies in a cookie store, or directly use it as a http client.
version: 0.0.1
repository: https://github.com/Dampfwalze/http_cookie_store

environment:
  sdk: ">=2.19.6 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
